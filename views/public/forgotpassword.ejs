<div class="row">
  <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
    <h1>Forgot Password</h1>
  </div>
  <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12">
    <h4>Enter your emailid here:</h4>
    <p>We will send new pasword in your registor emailid.</p>
    <div class="alert alert-danger" role="alert" ng-show="forgotAction.isInalidEmailId || forgotAction.isInalidEmailId_Server">
      <div ng-show="forgotAction.isInalidEmailId">
        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
        <span class="sr-only">Error:</span>
        Enter a valid email address
      </div>
      <div ng-show="forgotAction.isInalidEmailId_Server">
        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
        <span class="sr-only">Error:</span>
        No user foud.
      </div>
    </div>
    <!-- form method="post" action="/forgot" role="from" ng-submit="forgotAction.submitLogin($event)" novalidate="" -->
      <div class="form-group">
        <input name="email" type="text" class="form-control" placeholder="Email" ng-model="forgotAction.email" required="">
      </div>

      <div>
        <button type="button" class="btn btn-primary" ng-click="forgotAction.onSubmitClick();">Submit</button>
      </div>
    </div>
  <!-- /form -->

</div>
     
 